{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the PhiReader desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "updater:default",
    "process:allow-restart"
  ]
}
